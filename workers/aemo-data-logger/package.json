{
  "name": "aemo-data-logger",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler publish",
    "build": "echo \"No build step needed at this time.\"",
    "dev:logger": "wrangler dev --config wrangler.logger.toml",
    "dev:api": "wrangler dev --config wrangler.api.toml",
    "publish:logger": "wrangler publish --config wrangler.logger.toml",
    "publish:api": "wrangler publish --config wrangler.api.toml"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230322.1",
    "jsonwebtoken": "^9.0.0"
  }
}